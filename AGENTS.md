# Repo Rules

- Keep only `main` and `lab` as persistent local branches.
- Do not push changes directly to `main`.
- Push changes only to `lab` or to a new short-lived feature branch.
- Merge into `main` only from `lab` or from a reviewed feature branch.
